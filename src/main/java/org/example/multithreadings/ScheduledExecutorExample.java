package org.example.multithreadings;

public class ScheduledExecutorExample {
    public static void main(String[] args) {
      /*  ScheduledExecutorService scheduler = Executors.newScheduledThreadPool(1);

        Runnable task = () -> System.out.println("Scheduled Task executed");

        // Exécuter après un délai de 2 secondes
        scheduler.schedule(task, 2, TimeUnit.SECONDS);

        // Planifier une exécution répétée toutes les 3 secondes
        scheduler.scheduleAtFixedRate(task, 1, 3, TimeUnit.SECONDS);

        // Scheduler arrête après 10 secondes (simulation)
        scheduler.schedule(() -> scheduler.shutdown(), 10, TimeUnit.SECONDS);*/
    }
}

